# 実装計画

1. **README を example.md に近づける**
   - Hero / copy / contact を中心に情報を絞り、About・Tech・Contact が1スクロールで読める構成にする。
   - Devicon 等の skill icon を併用し、Markdown + inline images で表現する。

2. **docs をシンプル化**
   - `docs/requirements.md` と `docs/design.md` に example のアプローチ（About/Tech/Contact + スリムなセクション）を明示。
   - GitHub special repository の CSS 制約を再確認し、README でも文言で補足。

3. **docs-review でレンダリングレビューを記録**
   - Markdown-only レンダリングの確認結果を `docs-review/css-rendering-review.md` に記録し、どこまで反映できたか残す。
